# llm-json

Single-header C++17 JSON parser and builder with recursive-descent parsing.

## Structure
- `include/llm_json.hpp` — single-header implementation
- `examples/` — usage examples
- `CMakeLists.txt` — cmake build (requires vcpkg curl where applicable)

## Build
```bash
cmake -B build && cmake --build build
```
